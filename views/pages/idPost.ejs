<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>get post by id</h1>
     <li>
          <h3><%= posts[0].title %></h3>
          <p><%= posts[0].content %></p>
          <p> Author Name : <%= posts[0].authorName %></p>

          <p>Post creation Date <%= posts[0].created_at %></p>
          
        </li>
    
</body>
</html> -->










<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Post Details</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body class="bg-light">

  <div class="container mt-5">
    <div class="row justify-content-center">
      <div class="col-md-8">

        <% if (!posts || posts.length === 0) { %>
          <div class="alert alert-warning text-center">
            <h4 class="alert-heading">No Post Found</h4>
            <p>There is no post available with the provided ID.</p>
          </div>
        <% } else { %>
          <div class="card shadow">
            <div class="card-body">
              <h2 class="card-title"><%= posts[0].title %></h2>
              <p class="card-text"><%= posts[0].content %></p>
              <hr>
              <p><strong>Author:</strong> <%= posts[0].authorName %></p>
              <p><strong>Posted on:</strong> <%= new Date(posts[0].created_at).toLocaleString() %></p>
            </div>
          </div>
        <% } %>

        <div class="text-center mt-4">
          <a href="/dashboard" class="btn btn-outline-primary">Return to Dashboard</a>
        </div>

      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
