<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Update Your Post</h1>

<form action="/api/posts/updatePost?id=<%= posts[0].id %>" method="POST">
    <p>Post id :<%=posts[0].id %></p>
   <p> Current post title : <%= posts[0].title %> </p>
    <p>Current Post Content : <%= posts[0].content %>  </p>
  

  <label>New Title:</label><br>
  <input type="text" name="title" value="<%= posts.title %>" required><br><br>

  <label> New Content:</label><br>
  <textarea name="content" rows="6" cols="40" required><%= posts.content %></textarea><br><br>

  <button type="submit">Update</button>
</form>
<a href="/dashboard"> Return to dashboard</a>



</body>
</html> -->




<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Edit Blog Post</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  <div class="container mt-5">
    <h2 class="mb-4 text-center">Edit Your Blog Post</h2>

    <div class="mb-4">
      <p><strong>Post ID:</strong> <%= posts[0].id %></p>
      <p><strong>Current Title:</strong> <%= posts[0].title %></p>
      <p><strong>Current Content:</strong> <%= posts[0].content %></p>
    </div>

    <form action="/api/posts/updatePost?id=<%= posts[0].id %>" method="POST" class="border p-4 bg-white shadow-sm rounded">
      <div class="mb-3">
        <label for="title" class="form-label">New Title</label>
        <input type="text" class="form-control" id="title" name="title" value="<%= posts[0].title %>" required>
      </div>

      <div class="mb-3">
        <label for="content" class="form-label">New Content</label>
        <textarea class="form-control" id="content" name="content" rows="6" required><%= posts[0].content %></textarea>
      </div>

      <button type="submit" class="btn btn-primary">Update Post</button>
      <a href="/dashboard" class="btn btn-secondary ms-2">Back to Dashboard</a>
    </form>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
